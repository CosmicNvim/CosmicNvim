local config = {}
--[[ config.lsp = {
  format_on_save = true,
  servers = {
    eslint = false,
    efm = {
      format = false -- disable formatting all together
      disable_formatters = {'eslint', 'prettier', 'stylua'}
    },
   tsserver = {
      format = false,
   }
  }
} ]]

return config
