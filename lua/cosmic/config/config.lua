local config = {}
-- config.lsp = {
  -- format_on_save = true,
  -- servers = {
    -- eslint = false,
    -- efm = {
      -- disable_formatters = {'eslint', 'prettier', 'stylua'}
    -- }
  -- }
-- }

return config
